# Local development can omit DATABASE_URL and use SQLite in ./instance.
# Render sets DATABASE_URL automatically from the managed Postgres database.
DATABASE_URL=
SECRET_KEY=
MESSAGE_ENCRYPTION_KEY=
FLASK_ENV=development
