FLASK_ENV=development
SECRET_KEY=replace-with-a-long-random-secret
MESSAGE_ENCRYPTION_KEY=generate-with-Fernet
DATABASE_PATH=instance/stegovault.db
STEGOVAULT_DATA_DIR=instance

# First-run Admin bootstrap is configured by the UI, not by these variables.
